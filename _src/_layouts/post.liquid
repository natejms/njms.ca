<!DOCTYPE html>
<html>
    <head>
      <meta charset="utf-8">
      <meta http-equiv="X-UA-Compatible" content="IE=edge">
      <meta name="viewport" content="width=device-width, initial-scale=1">
      <title>njms - {{ page.title }}</title>

      {% include 'head.liquid' %}
      {% include 'opengraph.liquid' %}
    </head>
    <body>
    <div>
      <div class="container-fluid">
        <div class="row">
          {% include 'sidebar.liquid' %}
        </div>
        <div class="row">
          <div class="col-md-3 col-sm-3"></div>
            <div class="col-md-9 col-sm-9 main">
              <div class="main-content">
              <div class="post">
                <h1 class="post-title">{{ page.title }}</h1>
                <h2><small>{{ page.published_date }}</small></h2>
                <hr>
                <p>
                  <figure>
		    <img src="{{ page.data.thumbnail }}" alt="{{ page.data.alt_text }}">
                    <figcaption>
                      {{ page.data.caption }}
                    </figcaption>
                  </figure>
                  <br>
                  {{ page.content }}
		  <br/>
		  <h6 class="tldr">tl;dr</h6>
		  {{ page.description }}
                </p>
              </div>
            </div>
            {% include 'footer.liquid' %}
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
