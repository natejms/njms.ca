<div class="post">
  <h1 class="post-title">{{ title }}</h1>
  <h2><small>{{ timestamp }}</small></h2>
  <hr>
  <p>
    <figure>
      <img src="{{ thumbnail }}">
      <figcaption>
        {{ caption }}
      </figcaption>
    </figure>
    <br>
    {{ content }}
  </p>
</div>
